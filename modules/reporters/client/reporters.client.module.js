(function (app) {
  'use strict';

  app.registerModule('reporters');
  app.registerModule('reporters.admin');
  app.registerModule('reporters.admin.routes');
  app.registerModule('reporters.admin.services');
}(ApplicationConfiguration));
